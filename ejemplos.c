#include <stdio.h>

  int j(void); //declaracion de una variable

  int f(void) {
    printf("%s\n", "cavalcanti"); //side effect
    return "hola";
  }
  int suma(int a, int b) {
    return a + b;
}
